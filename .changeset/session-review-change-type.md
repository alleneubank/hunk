---
"hunkdiff": minor
---

Session and review payloads now report each file's `changeType`, so a client can tell an
added, deleted, or renamed file from a modified one without guessing from line counts.
